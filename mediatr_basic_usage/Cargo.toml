[package]
name = "mediatr_basic_usage"
version = "0.1.0"
edition = "2021"
description = "MediatR basic usage example - request/response pattern"

[dependencies]
mediatr = { path = "../mediatr" }
async-trait = "0.1"
tokio = { version = "1.0", features = ["rt-multi-thread", "macros"] }
